import { ViewStyle } from 'react-native';
export interface IconLabelProps {
  iconName: string;
  title: string;
}

export interface IconLabelStyles {
  container: ViewStyle;
  icon: ViewStyle;
  title: ViewStyle;
}
