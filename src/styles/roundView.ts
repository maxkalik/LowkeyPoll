import { ViewStyle } from 'react-native';
import { colors } from '../utils/constants';

const roundViewStyle: ViewStyle = {
  borderRadius: 12,
  paddingHorizontal: 20,
  paddingTop: 14,
  paddingBottom: 14,
  backgroundColor: colors.COLOR_DARK
};

export default roundViewStyle;
